import { memo } from "react";
import InnerBack from "../../../shared/components/ui/innerBack/innerBack";

const Statistic = () => {
  return (
    <>
      <title>Dashboard | Statistic</title>
      <InnerBack>Statistic</InnerBack>
    </>
  );
};

export default memo(Statistic);
